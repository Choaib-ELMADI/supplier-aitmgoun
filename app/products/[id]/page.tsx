export default function ProductPage({ params: id }: { params: { id: string }}) {
    const foo = id;

    return (
        <div className={ '' }>
            <h1>Product Page</h1>
        </div>
    );
};